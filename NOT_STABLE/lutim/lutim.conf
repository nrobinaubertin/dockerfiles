{
    hypnotoad => {
        listen => ['http://0.0.0.0:8080'],
    },
    always_encrypt                  => 1,
    anti_flood_delay                => 5,
    contact                         => '<CONTACT>',
    crypto_key_length               => 8,
    db_path                         => '/lutim/data/lutim.db',
    dbtype                          => 'sqlite',
    delete_no_longer_viewed_files   => 90,
    keep_ip_during                  => 365,
    length                          => 8,
    default_delay                   => <DEFAULT_DELAY>,
    max_delay                       => <MAX_DELAY>,
    max_file_size                   => <MAX_FILE_SIZE>,
    policy_when_full                => 'warn',
    prefix                          => '/',
    provis_step                     => 5,
    provisioning                    => 100,
    secrets                         => ['<SECRET>'],
    stats_day_num                   => 365,
    theme                           => 'default',
    token_length                    => 24,
};
